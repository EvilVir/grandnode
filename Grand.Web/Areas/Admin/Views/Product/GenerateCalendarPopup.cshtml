@model ProductModel.GenerateCalendarModel

@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminPopupLayout.cshtml";

    //page title
    ViewBag.Title = T("Admin.Catalog.Products.Calendar.Generatecalendar").Text;
}

<form asp-controller="Product" asp-action="GenerateCalendarPopup"
      asp-route-ProductId="@Context.Request.Query["ProductId"]"
      asp-route-btnId="@Context.Request.Query["btnId"]"
      asp-route-formId="@Context.Request.Query["formId"]">
    <antiforgery-token />

    <div class="row">
        <div class="col-md-12">
            <div class="x_panel light form-fit">
                <div class="x_title">
                    <div class="caption">
                        <i class="fa fa-cube"></i>
                        @T("Admin.Catalog.Products.Calendar.Generatecalendar")
                    </div>
                    <div class="actions">
                        <button class="btn btn-success" type="submit" name="save"><i class="fa fa-check"></i> @T("Admin.Common.Save") </button>
                        <vc:admin-widget widget-zone="product_details_calendar_create_buttons" additional-data="Model" />
                    </div>
                </div>
                <div class="x_content form">
                    <partial name="_GenerateCalendar" model="Model" />
                </div>
            </div>
        </div>
    </div>
</form>